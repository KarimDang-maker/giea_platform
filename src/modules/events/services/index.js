const eventService = require('./event.service');
const eventRegistrationService = require('./eventRegistration.service');
const eventSessionService = require('./eventSession.service');
const sessionParticipantService = require('./sessionParticipant.service');

module.exports = {
    eventService,
    eventRegistrationService,
    eventSessionService,
    sessionParticipantService
};
